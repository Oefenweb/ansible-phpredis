# vars file for phpredis
---
phpredis_dependencies:
  - "{{ 'php7.0-dev' if (ansible_distribution == 'Ubuntu' and ansible_distribution_version | version_compare('16.04', '>=')) else 'php5-dev' }}"
  - build-essential
  - git-core
